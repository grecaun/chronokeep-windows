<ui:FluentWindow x:Class="Chronokeep.UI.Timing.ClockControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chronokeep.UI.API"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Remote Readers"
        d:DataContext="{d:DesignInstance local:ClockControl, IsDesignTimeCreatable=True}"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        Height="400"
        Width="1090"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        Closed="Window_Closed"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ui:TitleBar
            x:Name="titleBar"
            Grid.Row="0">
        </ui:TitleBar>
        <StackPanel Grid.Row="1" Width="1090">
            <ListView
                x:Name="clockListView"
                Height="400"
                Margin="10,0,10,10"
                ItemContainerStyle="{DynamicResource NoFocusListViewItem}"
                />
            <Button x:Name="AddButton" Content="Add Clock" Click="AddButton_Click" Height="35" Width="150" HorizontalAlignment="Center" Margin="20,0,10,20" />
            <TextBlock Name="CurrentTimeLabel" VerticalAlignment="Center" TextAlignment="Center" Text="" FontSize="14" Margin="10,5,10,5"/>
            <TextBlock Name="TimeLabel" VerticalAlignment="Center" TextAlignment="Center" Text="" FontSize="14" Margin="10,5,10,5"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="CloseButton" Content="Close" Click="Close_Click" Height="35" Width="150" HorizontalAlignment="Center" Margin="20,0,10,20" />
            </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
